# Application
APP_NAME="Secure Online Transaction System"
APP_ENV=development
APP_DEBUG=true
BASE_URL=http://localhost/MPWT/

# MongoDB Atlas
MONGO_URI=mongodb+srv://user:pass@cluster0.mongodb.net/
DB_NAME=secure_ot_system

# Gmail SMTP for OTP (primary)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=vinothyokesh008009@gmail.com
SMTP_PASSWORD=hxpdlesdpctsvdmg
SMTP_FROM_NAME="Secure Online Transaction System"
SMTP_SECURE=tls

# Brevo HTTPS email API (fallback when SMTP is blocked on the host)
# Render free tier blocks SMTP but not HTTPS (port 443). Set in Render dashboard.
# Free key: https://www.brevo.com  ->  SMTP & API  ->  API Keys
BREVO_API_KEY=
BREVO_FROM_EMAIL=your-verified-sender@example.com

# Brevo HTTPS email API (fallback when SMTP is blocked on the host)
# Free key from https://www.brevo.com (SMTP & API -> API Keys)
BREVO_API_KEY=
BREVO_FROM_EMAIL=your-verified-sender@example.com

# Security
SESSION_LIFETIME=3600
SECRET_KEY=change-me
ENCRYPTION_KEY=change-me
HASH_COST=12
PASSWORD_MIN_LENGTH=8
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=900